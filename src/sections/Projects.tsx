import React from "react";
import Title from "../components/Title";

const Projects = () => {
    return (
        <section id="projects">
            <Title text="Projects" />
        </section>
    );
};

export default Projects;